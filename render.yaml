services:
  - type: web
    name: silero-vad-rust-ort
    runtime: docker
    plan: free
    region: singapore
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: RUST_LOG
        value: rust_ort_vad=info
